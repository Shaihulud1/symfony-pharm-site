<form method="post" action="{{ path('bonus_delete', {'id': bonus.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <a href="{{ path('bonus_edit', {'id': bonus.id}) }}" class="btn btn-info">Изменить</a>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bonus.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
