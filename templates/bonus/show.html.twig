{% extends 'base.admin.html.twig' %}

{% block title %}Bonus{% endblock %}

{% block body %}
    <h1>Bonus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bonus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bonus.name }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ bonus.text }}</td>
            </tr>
            <tr>
                <th>Label_text</th>
                <td>{{ bonus.labelText }}</td>
            </tr>
            <tr>
                <th>Label_color</th>
                <td>{{ bonus.labelColor }}</td>
            </tr>
            <tr>
                <th>Title_color</th>
                <td>{{ bonus.titleColor }}</td>
            </tr>
            <tr>
                <th>Bonus_pic</th>
                <td>{{ bonus.bonusPic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bonus_index') }}">back to list</a>

    <a href="{{ path('bonus_edit', {'id': bonus.id}) }}">edit</a>

    {{ include('bonus/_delete_form.html.twig') }}
{% endblock %}
