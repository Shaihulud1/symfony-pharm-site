{% extends 'base.admin.html.twig' %}

{% block title %}Edit Bonus{% endblock %}

{% block body %}
    <h1>Edit Bonus</h1>

    {{ include('bonus/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('bonus_index') }}" class="btn btn-primary">К списку</a>

    {{ include('bonus/_delete_form.html.twig') }}
{% endblock %}
