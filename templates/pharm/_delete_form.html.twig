<form method="post" action="{{ path('pharm_delete', {'id': pharm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <a href="{{ path('pharm_edit', {'id': pharm.id}) }}" class="btn btn-info">Изменить</a>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharm.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
