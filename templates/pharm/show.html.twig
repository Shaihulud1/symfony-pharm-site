{% extends 'base.admin.html.twig' %}

{% block title %}Pharm{% endblock %}

{% block body %}
    <h1>Pharm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pharm.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pharm.name }}</td>
            </tr>
            <tr>
                <th>Pharm_pic</th>
                <td>{{ pharm.pharmPic }}</td>
            </tr>
            <tr>
                <th>Coords</th>
                <td>{{ pharm.coords }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ pharm.type }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ pharm.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pharm_index') }}">back to list</a>

    <a href="{{ path('pharm_edit', {'id': pharm.id}) }}">edit</a>

    {{ include('pharm/_delete_form.html.twig') }}
{% endblock %}
