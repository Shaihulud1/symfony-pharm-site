{% extends 'base.admin.html.twig' %}

{% block title %}AboutLogo{% endblock %}

{% block body %}
    <h1>AboutLogo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about_logo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ about_logo.name }}</td>
            </tr>
            <tr>
                <th>Logo_pic</th>
                <td>{{ about_logo.logoPic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('about_logo_index') }}">back to list</a>

    <a href="{{ path('about_logo_edit', {'id': about_logo.id}) }}">edit</a>

    {{ include('about_logo/_delete_form.html.twig') }}
{% endblock %}
