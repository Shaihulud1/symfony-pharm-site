{% extends 'base.admin.html.twig' %}

{% block title %}Advantage{% endblock %}

{% block body %}
    <h1>Advantage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advantage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ advantage.name }}</td>
            </tr>
            <tr>
                <th>Adv_pic</th>
                <td>{{ advantage.advPic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('advantage_index') }}">back to list</a>

    <a href="{{ path('advantage_edit', {'id': advantage.id}) }}">edit</a>

    {{ include('advantage/_delete_form.html.twig') }}
{% endblock %}
