{% extends 'base.admin.html.twig' %}

{% block title %}Action{% endblock %}

{% block body %}
    <h1>Action</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ action.name }}</td>
            </tr>
            <tr>
                <th>Action_pic</th>
                <td>{{ action.actionPic }}</td>
            </tr>
            <tr>
                <th>Logo_pic</th>
                <td>{{ action.logoPic }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ action.text }}</td>
            </tr>
            <tr>
                <th>Text_color</th>
                <td>{{ action.textColor }}</td>
            </tr>
            <tr>
                <th>Label_color</th>
                <td>{{ action.labelColor }}</td>
            </tr>
            <tr>
                <th>Label_text</th>
                <td>{{ action.labelText }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ action.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('action_index') }}">back to list</a>

    <a href="{{ path('action_edit', {'id': action.id}) }}">edit</a>

    {{ include('action/_delete_form.html.twig') }}
{% endblock %}
